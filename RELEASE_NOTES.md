Download **OneTapDND.apk** below and open it on your Android phone to install.

**Upgrading from v1.1 or v1.1.1:** install this APK over the existing app. It uses the same signing key and keeps your settings.

**Upgrading from v1.0:** uninstall v1.0 first because its signing key differs, then grant DND access and add the tile again.

- Add a quiet place from a Maps screenshot. On-device text recognition extracts the place name, address, plus code, or coordinates and fills the form.
- Address parsing covers common international address and coordinate formats while filtering map controls, ratings, hours, phone numbers, and status-bar text.
- Dark mode now uses a pure black background and surfaces.
- The white launcher icon now has a black background, including the round variant.
- The selected icon-style checkmark now appears to the right of its label.

Always preview an extracted location before saving it. Quiet places require Google Play services, precise location, and background location access. Android can take a few minutes to detect an arrival or departure. Reopen the app after force-stopping it.
